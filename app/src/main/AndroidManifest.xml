<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="destroxwhey.de.socialalarm" >

    <application
        android:name =".SocialAlarm"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".OverviewActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity_Alarm"
            android:label="@string/title_activity_activity__alarm"
            android:parentActivityName=".OverviewActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="destroxwhey.de.socialalarm.OverviewActivity" />
        </activity>
        <receiver android:name=".NotifyService"></receiver>
    </application>

</manifest>
